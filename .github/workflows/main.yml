# demo

name: demo

on:
    push:
        branches:
            - cmyjim-patch-1
        paths:
            - '.github/workflows/**'
            - 'src/**'
jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
            - name: use Node.js
              uses: actions/setup-node@v1
              with:
                  node-version: 16
            - name: print node version
              run: |
                  node -v
                  npm -v
                  echo "hello jim"